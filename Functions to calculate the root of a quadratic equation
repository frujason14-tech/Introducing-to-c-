#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a, b, c;
    cin >> a >> b >> c;

    double d = b*b - 4*a*c;

    if (d >= 0) {
        cout << "Root 1: " << (-b + sqrt(d)) / (2*a) << endl;
        cout << "Root 2: " << (-b - sqrt(d)) / (2*a) << endl;
    } else {
        cout << "Complex roots" << endl;
}
return 0;
}
